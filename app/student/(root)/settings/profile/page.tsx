import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Settings - Profile",
    description: "User profile Settings"
}
function ProfileSettins() {
    return (
        <div>ProfileSettins</div>
    )
}

export default ProfileSettins